[tool.poetry]
name = "zakupai_common"
version = "0.1.0"
description = "Shared lib for ZakupAI services"
authors = ["ZakupAI Team <team@zakup.ai>"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.104.0"
pydantic-settings = "^2.0.3"
aiohttp = "^3.8.5"  # Для будущих retry в API
