# 🧹 Система автоочистки логов ZakupAI

## Обзор

Система автоматической очистки логов обеспечивает:

- **Контроль размера базы данных** - не позволяет ей расти бесконечно
- **Автоматическое удаление старых записей** - по времени и размеру
- **Telegram команды для админов** - удобное управление через бот
- **Cron автоматизация** - запуск очистки по расписанию
- **Детальное логирование операций** - отслеживание всех действий

## 🚀 Возможности

### **Автоматическая очистка**

- **По времени**: удаление записей старше N дней (по умолчанию 90)
- **По размеру**: автоочистка при превышении лимита (по умолчанию 100MB)
- **Умная стратегия**: автоматический выбор периода очистки
- **VACUUM оптимизация**: освобождение места на диске

### **Telegram команды**

```bash
/dbinfo                    # Информация о размере базы
/cleanup [дни]            # Очистка логов старше N дней
/autocleanup [размер_MB]  # Автоочистка при превышении размера
```

### **Cron автоматизация**

```bash
# Ежедневная очистка в 2:00 утра
0 2 * * * /path/to/cleanup_logs.py

# Еженедельная агрессивная очистка
0 3 * * 0 /path/to/cleanup_logs.py
```

## 📊 Стратегии очистки

### **Умная очистка по размеру**

- **< 50 MB**: 🟢 Очистка не требуется
- **50-100 MB**: 🟡 Удаление записей старше 60 дней
- **> 100 MB**: 🔴 Удаление записей старше 30 дней

### **Умная очистка по количеству**

- **< 10,000 записей**: Стандартная очистка (90 дней)
- **10,000-50,000 записей**: Умеренная очистка (60 дней)
- **> 50,000 записей**: Агрессивная очистка (30 дней)

## 🔧 Настройка

### **1. Настройка админов**

```python
# В handlers_v2.py обновите admin_ids
admin_ids = {YOUR_TELEGRAM_ID, ANOTHER_ADMIN_ID}
```

### **2. Настройка автоочистки**

```python
# В user_metrics.py измените параметры
max_size_mb = 150  # Лимит размера базы
days_to_keep = 90  # Дней для сохранения
```

### **3. Настройка cron**

```bash
# Откройте crontab
crontab -e

# Добавьте строку (замените путь на свой)
0 2 * * * /path/to/zakupai/cleanup_logs.py >/dev/null 2>&1
```

## 📋 Мониторинг

### **Логи операций**

```bash
# Логи автоочистки
tail -f data/cleanup.log

# Логи cron операций
tail -f data/cleanup_cron.log
```

### **Проверка статуса базы**

```python
from user_metrics import metrics_service

# Информация о базе данных
info = metrics_service.get_database_info()
print(f"Размер: {info['db_size_mb']:.2f} MB")
print(f"Записей: {info['total_records']:,}")
```

## 🛠️ API функций

### **UserMetricsService**

```python
# Очистка старых логов
cleanup_stats = metrics_service.cleanup_old_logs(days_to_keep=90)

# Информация о базе
db_info = metrics_service.get_database_info()

# Автоочистка по размеру
result = metrics_service.auto_cleanup_by_size(max_size_mb=100)
```

### **AnalyticsService**

```python
# Форматированный отчет очистки
report = analytics_service.cleanup_old_logs(days_to_keep=90)

# Информация о базе для Telegram
info_text = analytics_service.get_database_info_text()

# Автоочистка с отчетом
auto_report = analytics_service.auto_cleanup_by_size(max_size_mb=100)
```

## 🚨 Важные особенности

### **Безопасность**

- ✅ **Минимальный период**: нельзя удалить записи младше 7 дней
- ✅ **Максимальный период**: лимит 365 дней для очистки
- ✅ **Размер лимиты**: от 10 MB до 1000 MB
- ✅ **Транзакции**: атомарные операции в базе данных

### **Производительность**

- ✅ **VACUUM**: автоматическая дефрагментация базы
- ✅ **Индексы**: сохраняются после очистки
- ✅ **Batching**: эффективная обработка больших объемов

### **Мониторинг**

- ✅ **Детальная статистика**: количество удаленных записей, размер освобожденного места
- ✅ **Логирование ошибок**: все ошибки записываются в файл
- ✅ **Telegram уведомления**: отчеты прямо в бот

## 📊 Примеры использования

### **Ежедневная проверка размера**

```python
db_info = metrics_service.get_database_info()
if db_info['db_size_mb'] > 100:
    print("🔴 База данных превышает 100 MB - требуется очистка")
    cleanup_stats = metrics_service.cleanup_old_logs(days_to_keep=60)
    print(f"✅ Удалено {cleanup_stats['deleted_count']} записей")
```

### **Telegram команда проверки**

```python
@router.message(Command("checkdb"))
async def check_db_command(message: Message):
    db_info = analytics_service.get_database_info_text()
    await message.answer(db_info, parse_mode="Markdown")
```

### **Автоматическое уведомление админов**

```python
async def weekly_cleanup_report():
    cleanup_stats = metrics_service.cleanup_old_logs(days_to_keep=90)
    if cleanup_stats['deleted_count'] > 0:
        report = f"🧹 Еженедельная очистка: удалено {cleanup_stats['deleted_count']} записей"
        # Отправить админам через Telegram
```

## 💡 Рекомендации

### **Для небольших проектов (< 1000 пользователей)**

- Очистка раз в неделю
- Хранение логов 90 дней
- Лимит базы данных 50 MB

### **Для средних проектов (1000-10000 пользователей)**

- Очистка каждый день
- Хранение логов 60 дней
- Лимит базы данных 100 MB

### **Для крупных проектов (> 10000 пользователей)**

- Очистка каждые 6 часов
- Хранение логов 30 дней
- Лимит базы данных 200 MB
- Переход на PostgreSQL

______________________________________________________________________

## ✅ Готово к использованию!

Система автоочистки полностью настроена и готова к работе в продакшене. Все функции протестированы и документированы.

**Следующие шаги:**

1. Настроить админские ID в `handlers_v2.py`
1. Добавить cron задачу для автоматической очистки
1. Мониторить размер базы данных через команды бота
1. При необходимости настроить параметры очистки
